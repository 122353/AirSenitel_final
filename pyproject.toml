[project]
name = "vayunirikshak-evidence-api"
version = "2.0.0"
requires-python = "~=3.12.0"
dependencies = [
  "fastapi==0.141.1",
  "uvicorn==0.53.0",
  "httpx==0.28.1",
  "python-multipart==0.0.32",
  "clerk-backend-api==6.0.1",
  "psycopg[binary]==3.3.6",
  "Pillow==12.3.0",
  "python-dotenv==1.2.3",
]

[tool.vercel]
entrypoint = "api_service:app"

[tool.uv]
package = false
